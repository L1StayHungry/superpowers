**Implementer dispatch prompts: point, don’t paste.** If a task brief exists on disk, do not restate its plan/spec/context in the prompt, including inside code blocks. Put the brief path, required report path, and exact invocation/command near the top. Include only constraints not already in the brief or needed to route the agent. For `/tmp/sdd/task-03-brief.md`, require output to `/tmp/sdd/task-03-report.md`.
