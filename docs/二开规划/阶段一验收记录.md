# 阶段一验收记录

日期：2026-05-09 / 2026-05-10 / 2026-05-11
分支：`t-dev`
Claude Code：`2.1.132`

## 当前状态

阶段一状态：`完成 / ready for stage 2`。

阶段二可以开始；开 PR 前仍必须按 `AGENTS.md` 执行 PR 模板阅读、历史 PR 查重、完整 diff 人审和明确批准。当前静态迁移自检、排除文件保护、阶段一测试硬门槛、subagent smoke harness、Type A/B transcript、Claude Code Type C co-installed transcript 和 Codex hook 决策均已达标。

## 验收标准修订

阶段一硬指标正式修订：`svelte-todo` 与 `go-fractals` 不再作为阶段一完成的硬阻塞项，改为 pressure fixtures。原因是这两个长样例主要验证长时间多代理编排稳定性、最终 review 时长和复杂任务吞吐，不等同于阶段一命名迁移正确性。

阶段一新的 subagent 硬门槛是 `tests/subagent-driven-dev/run-test.sh t-smoke --plugin-dir /Users/lihuajun/WorkProject/superpowers --timeout 900` 退出码 0。该 smoke fixture 覆盖 `t-superpowers:t-subagent-driven-development` 触发、`Read` 工具禁用、worktree 合回、主项目提交、生成项目测试通过和真实退出码传播。

2026-05-11 修订：阶段一 harness transcript 范围收窄为 Claude Code CLI 与 Codex CLI。Cursor.app 与 Codex App 真实 transcript 不再作为阶段一完成硬阻塞项；Cursor 侧只保留 manifest 结构静态校验与占位目录要求。

2026-05-11 追加修订：Codex CLI co-installed 验证从阶段一硬门槛降级为后续验证项。原因是当前临时 `CODEX_HOME` 同时安装 official 与 fork 时，Codex CLI 只识别 official marketplace 安装，无法把手工塞入的 fork 缓存识别为第二个已安装插件。阶段一只要求记录该限制；Codex CLI 的 official-only 与 fork-only 单装行为仍必须通过。

本修订不影响 Type A / Type B / Type C 的验收类别，也不影响 Codex hook 决策要求。

## 硬指标复核

| 硬指标 | 状态 | 证据 |
| --- | --- | --- |
| `tools/t-stage1-check.sh` 退出码 0 | 通过 | `OK: Stage 1 migration self-check passed` |
| 6 个排除文件无 diff | 通过 | `git diff --exit-code -- package.json gemini-extension.json .opencode/plugins/superpowers.js CLAUDE.md AGENTS.md GEMINI.md` 退出码 0 |
| 三个替换范围内测试套件全部通过 | 通过 | Brainstorm server、Claude Code targeted/integration 和 Subagent smoke harness `t-smoke` 已通过；`svelte-todo` / `go-fractals` 按修订标准保留为 pressure fixtures |
| Subagent smoke harness `t-smoke` | 通过 | `tests/subagent-driven-dev/run-test.sh t-smoke --plugin-dir /Users/lihuajun/WorkProject/superpowers --timeout 900` 退出码 0 |
| 三类 harness transcript 均已存档 | 通过 | Type A/B 在 Claude Code CLI + Codex CLI 范围内通过；Type C 的 Claude Code CLI co-installed 通过；Codex CLI co-installed 已降级为后续验证项并保留失败证据 |

## 已通过

- `bash tools/t-stage1-check.sh`：已通过。
- 排除范围保护：`package.json`、`gemini-extension.json`、`.opencode/plugins/superpowers.js`、`CLAUDE.md`、`AGENTS.md`、`GEMINI.md` 无迁移 diff。
- Brainstorm server：
  - `npm test`：25 pass，0 fail。
  - `npm test` 连续稳定性复跑：20 次连续通过，未复现 WebSocket upgrade 偶发失败。
  - `node ws-protocol.test.js`：31 pass，0 fail。
  - `bash windows-lifecycle.test.sh`：8 pass，0 fail，3 skipped。
  - `OSTYPE=msys bash windows-lifecycle.test.sh`：11 pass，0 fail。
  - `MSYSTEM=MINGW64 bash windows-lifecycle.test.sh`：11 pass，0 fail。
- Claude Code targeted tests：
  - `test-document-review-system.sh`：pass。
  - `test-requesting-code-review.sh --integration`：pass。
  - `test-subagent-driven-development.sh`：pass。
  - `test-subagent-driven-development-integration.sh --integration`：pass。
    - 命令：`bash tests/claude-code/run-skill-tests.sh --integration --test test-subagent-driven-development-integration.sh --verbose`
    - 结果：`Passed: 1 / Failed: 0 / Skipped: 0`，耗时 673s。
    - transcript：`/Users/lihuajun/.claude/projects/-private-var-folders-qb-20ll8fzs1ps454544pv-74j00000gn-T-tmp-o9K5qfIIm9/d3474b7e-b952-481a-b0fb-4807fa978918.jsonl`
    - 验证点：7 个 subagent dispatch、TodoWrite 使用、`src/math.js` 中 `add`/`multiply` 存在、测试 6 pass / 0 fail、3 个提交、无额外 math feature。
    - transcript 中未出现 `Invalid pages` 或 `"name":"Read"`。
  - 新增本地回归：
    - `bash tests/claude-code/run-skill-tests-timeout.test.sh`：pass。
    - `bash tests/claude-code/test-helpers-timeout.test.sh`：pass。
    - `bash tests/claude-code/test-helpers-plugin-dir.test.sh`：pass。
    - `bash tests/claude-code/test-subagent-driven-development-integration-prompt.test.sh`：pass。
    - `bash tests/subagent-driven-dev/run-test-preflight.test.sh`：pass。
- Subagent smoke harness：
  - 命令：`tests/subagent-driven-dev/run-test.sh t-smoke --plugin-dir /Users/lihuajun/WorkProject/superpowers --timeout 900`
  - 结果：Claude exit code 0；fixture verifier exit code 0；`OK: t-smoke verification passed`。
  - 项目：`/tmp/superpowers-tests/1778388529/subagent-driven-development/t-smoke/project`
  - transcript：`/tmp/superpowers-tests/1778388529/subagent-driven-development/t-smoke/claude-output.json`
  - 生成项目独立复核：`npm test` 2 pass / 0 fail。
  - 主项目提交：`bd26805 Add Node project skeleton`、`c5f8f73 Add tested math helper`。
  - 生成项目 `git status --short` 无输出。
  - transcript 中未出现 `Invalid pages parameter`、`"name": "Read"` 或 `Read(`。
- Go 工具链：
  - 已通过 Homebrew 安装并确认可用：`go version go1.26.3 darwin/arm64`。

## 未通过 / 阻塞

- Brainstorm server `npm test` 曾在复跑中出现一次 WebSocket upgrade 偶发失败：第一次 24 pass / 1 fail，第二次 25 pass / 0 fail；后续 20 次连续重跑未复现。
  - 该项不是阶段一硬指标的直接失败项，目前按已记录风险处理。
  - 若后续 CI 或验收再复现，应修复启动、监听或 WebSocket 握手时序 race。

## Pressure Fixtures / 非阻塞风险

`svelte-todo` 与 `go-fractals` 保留为 failed pressure runs，直到后续 exit-0 运行取代以下记录。两者用于记录长时间多代理编排、最终 review 时长和复杂任务吞吐风险，不再作为阶段一完成硬阻塞项。

- `tests/subagent-driven-dev/svelte-todo` pressure run 仍失败。
  - 最新复跑命令：`tests/subagent-driven-dev/run-test.sh svelte-todo --plugin-dir /Users/lihuajun/WorkProject/superpowers --timeout 3600`，外层 3600 秒超时，退出码 124。
  - 最新日志：`/tmp/superpowers-tests/1778313903/subagent-driven-development/svelte-todo/claude-output.json`
  - 最新项目：`/tmp/superpowers-tests/1778313903/subagent-driven-development/svelte-todo/project`
  - 本次已加入 Node 版本前置检查、`Read` 工具禁用与 fixture 指令；最新日志中未出现 `Invalid pages` 或 `"name":"Read"`，说明原 `Read` schema 循环已被避开。
  - 进展已明显改善：主测试项目已有提交到 Task 8，最新提交 `407dfd1 Integrate todo app components`，之后 Task 9 已派发但未在 3600 秒内完成。
  - 主测试项目除 `.claude/worktrees/` 外无业务文件脏改动，说明 worktree 合回流程已改善；但 12-task 原 fixture 仍未完整结束，只能作为 pressure 风险记录。
  - 旧日志仍保留：`/tmp/superpowers-tests/1778301471/subagent-driven-development/svelte-todo/claude-output.json`
- `tests/subagent-driven-dev/go-fractals` pressure run 仍失败。
  - 最新复跑命令：`tests/subagent-driven-dev/run-test.sh go-fractals --plugin-dir /Users/lihuajun/WorkProject/superpowers --timeout 3600`，外层 3600 秒超时，退出码 124。
  - 最新日志：`/tmp/superpowers-tests/1778317530/subagent-driven-development/go-fractals/claude-output.json`
  - 最新项目：`/tmp/superpowers-tests/1778317530/subagent-driven-development/go-fractals/project`
  - Go 环境阻塞已解除：`go version go1.26.3 darwin/arm64`。
  - 最新运行已完成 10 个任务的主项目提交，末尾提交 `cb96176 Add README usage documentation`；Task 10 规格审查通过，外层超时发生在最终整体 code review 期间。
  - 超时后人工补充验证：`cd /tmp/superpowers-tests/1778317530/subagent-driven-development/go-fractals/project && go test ./...` 通过 `cmd/fractals`、`internal/cli`、`internal/mandelbrot`、`internal/sierpinski`。
  - 最新日志中未出现 `Invalid pages parameter`、`"name":"Read"` 或 `Read(`；主测试项目除 `.claude/worktrees/` 外无业务文件脏改动。
  - 但外层 harness 退出码仍为 124，最终整体 review 未在 timeout 内完成，只能作为 pressure 风险记录。

## Harness Acceptance

Type A official-only baseline：通过，记录见 `docs/二开规划/harness-transcripts/type-a-official-only.md`。
- Claude Code CLI：通过。`SessionStart` hook 注入 `superpowers:using-superpowers`，随后自动调用 `superpowers:brainstorming`。
- Codex CLI：通过。自动读取 `superpowers:using-superpowers` 与 `superpowers:brainstorming`；未出现 Codex hook/session-start 事件。
- Cursor.app / Codex App：按 2026-05-11 修订，不再纳入阶段一 transcript 硬指标。

Codex hook 决策已关闭为选项 B：Type A Codex CLI 证明 official baseline 没有 hook 注入，且 `codex features list` 显示 `codex_hooks` 为 `false`。因此 `.codex-plugin/plugin.json` 已切为 `"hooks": []`，并删除 `hooks/hooks-codex.json`。切换后 `bash tools/t-stage1-check.sh` 已通过。

Type B fork-only：通过，记录见 `docs/二开规划/harness-transcripts/type-b-fork-only.md`。
- Claude Code CLI：通过。`--plugin-dir /Users/lihuajun/WorkProject/superpowers` 观察到 `t-superpowers:t-using-superpowers` bootstrap 注入，技能列表为 `t-superpowers:t-*`。
- Codex CLI：通过。临时 `CODEX_HOME` 中加载最终 fork 内容后，出现 `t-superpowers:t-using-superpowers` 与 `t-superpowers:t-brainstorming`，未出现 official namespace 或 hook 事件。
- Cursor.app / Codex App：按 2026-05-11 修订，不再纳入阶段一 transcript 硬指标。

Type C co-installed：阶段一通过，记录见 `docs/二开规划/harness-transcripts/type-c-co-installed.md`。
- Claude Code CLI：通过。official 与 fork 同装时两个 namespace 均可见，双 bootstrap 已记录为 co-installed 事实。
- Codex CLI：后续验证项。同一临时 `CODEX_HOME` 同时放置 official 与 fork 缓存时，Codex CLI 只加载 official namespace，`t-superpowers:t-*` 计数为 0；该安装模型限制已记录，不再阻塞阶段一。
- Cursor.app / Codex App：按 2026-05-11 修订，不再纳入阶段一 transcript 硬指标。

## 后续跟踪

- 继续记录 `tests/subagent-driven-dev/svelte-todo` pressure fixture：`Read` 工具 `pages: ""` 循环已未复现，worktree 合回也已改善，但原 12-task fixture 仍在 Task 9 前后超时；后续 exit-0 运行可取代当前 failed pressure run 记录。
- 继续记录 `tests/subagent-driven-dev/go-fractals` pressure fixture：Go 环境已解决，生成项目测试通过，但外层 harness 仍因最终整体 review 超时退出 124；后续 exit-0 运行可取代当前 failed pressure run 记录。
- 解决 Codex CLI co-installed 加载不到 `t-superpowers` 的安装/marketplace 限制，补一条同时可见 `superpowers` 与 `t-superpowers` namespace 的 transcript；该项作为后续验证，不阻塞阶段二。
- 若后续复现 brainstorm-server WebSocket upgrade flake，先修复再收尾。

结论：阶段一静态迁移、Claude Code integration、brainstorm-server 稳定性复跑、本地 harness 回归和新的 subagent 硬门槛 `t-smoke` 已收口。`svelte-todo` 与 `go-fractals` 都已避开旧的 `Read` 循环并改善 worktree 合回，但两者最新复跑仍以退出码 124 超时结束，作为 pressure fixtures 继续记录。Codex hook 决策已按 Type A Codex CLI 证据关闭为 `hooks: []`。Cursor.app / Codex App transcript 已移出阶段一硬门槛；Codex CLI co-installed 已降级为后续验证项。按当前修订后的硬指标，阶段一可标记为完成并准备进入阶段二。

## 2026-07-10 后续勘误：Codex hooks 空值形状

阶段一在当时的官方基线与本地 Codex 能力证据下，使用 `"hooks": []` 表示不注册 SessionStart hook；上述记录保留为历史验收事实，不做回写。

upstream v6.1.1 已将 Codex manifest 的禁用 hook 形状修正为精确的空对象 `"hooks": {}`，用于避免 SessionStart hook 被错误重新注册。当前 fork 从 `20260710-upstream-6-1-1-runtime` 起同步采用 `{}`，`tools/t-stage1-check.sh` 与 npm 打包回归也只接受 `{}`。这次勘误不改变阶段一关于“不为 Codex 注入 SessionStart”的行为结论，只更新 manifest schema 表达。
